Run `tsk doctor` and report each check line plus the overall verdict. If any check FAILs, give the one-line fix for it:

- `config present` / `~/.tsk writable` — run `tsk init` once.
- `settings.json wired` — run `tsk init`.
- `tsk in PATH` — add the directory containing `tsk` to PATH.